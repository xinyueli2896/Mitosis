% ---------------------------------------------------------------
% Methodology. Three subsections matching the three designs named in
% the introduction:
%   3.2 interleaving: one sequence, one clock, rotary at the frame
%   3.3 attention: three key sets, three passes, two gates
%   3.4 per-stream expert routing  (\input method_per_part_moe.tex)
%   3.5 order-free joint prediction: query tokens + refinement
% 3.3 was split out of 3.2 on 2026-09-12 by request. The backbone
% material (two projection sets and where they come from) went WITH the
% attention, so 3.2 is now about the input sequence alone.
%
% THE THREE KEY SETS -- intra, cross, within-frame, one paragraph each
% (2026-09-12, by request: introduce the attention from those three
% perspectives) -- are the paper's statement of _build_masks in
% cp_transformer_m2c_duet_block.py. They
% are DISJOINT and their union is every position with prediction frame
% <= t -- check any edit against that, since the "partition" claim and
% the gate-to-zero identity both rest on it. The code's clean-clean
% rows: intra = same_mod & causal_pos, cross = diff_mod & f_q < f_p,
% frame = diff_mod & f_q == f_p. Query-token rows differ (the code's
% query slots; see 3.5) and are deliberately NOT described here.
%
% NO \paragraph HEADINGS (2026-09-12, by request). The transitions that
% the headings used to carry now sit in the prose -- if a paragraph is
% split or reordered, re-check its opening sentence.
%
% TERMINOLOGY -- one word per concept, fixed 2026-09-12. If an edit
% needs a new word for any of these, change it everywhere.
%   stream   melody or chord; x and y in the general setup. "modality"
%            belongs to the general framework and the positioning
%            clause only (EXPERIMENTS.md Decision 1, amended 2026-09-04).
%   frame    ONE STEP OF THE SHARED CLOCK, carrying one TOKEN of each
%            stream. NOT a per-stream unit: the within-frame connection
%            joins the two streams AT a frame, which is unsayable
%            otherwise. ("edge" was retired 2026-09-13 -- the paper
%            builds no graph vocabulary anywhere else.)
%   position ABSOLUTE index into the interleaved sequence, p = 1..2T --
%            two per frame, and NOT a time index. Frame t occupies
%            positions 2t-1 and 2t IN EQ. 1 -- the CONTENT sequence.
%            The model reads it SHIFTED, so position p then holds what
%            p-2 carries -- its OWN STREAM's token one frame earlier,
%            not a whole frame, which is two tokens -- and predicts the
%            token p itself carries. Do not state the
%            unshifted map and the shifted one as if they were the
%            same sequence.
%            PARITY flips with the indexing base. Paper (1-indexed,
%            per Eq. 1): odd p = x = melody, even p = y = chord. Code
%            (0-indexed): h[:, 0::2] is mod_a = melody, h[:, 1::2] is
%            mod_b = chord, i.e. EVEN = melody. Same fact -- melody is
%            the first entry of a frame -- so do not "fix" either to
%            match the other. RoPE is applied at the PREDICTION FRAME
%            ceil(p/2), NOT at p: the diffusion model overrides
%            _run_global_stack and halves the position vector under
%            time_rope_aligned (v1.2), so x_t and y_t share a rotary
%            position and the slots land on t+1. Everything is
%            1-indexed, hence ceil(p/2) for the prediction frame --
%            the code is 0-indexed and uses p//2. Do NOT also use
%            "position" in the metrical sense (say a frame is "one
%            sixteenth note long").
%   token    what ONE stream carries at ONE frame: all its simultaneous
%            notes, collapsed by the local encoder to the single vector
%            the outer transformer handles. In the testbed, a MELODY
%            TOKEN or a CHORD TOKEN -- the two words the paper needs
%            most, which is why they get the short name (decided
%            2026-09-13). Defined at first use in 3.1 because in
%            symbolic music "token" normally means the NOTE event; here
%            the note is a sub-token and the token is the frame-level
%            vector. The code disagrees: its `token` is our sub-token.
%            EVERY position carries a token. Two kinds (2026-09-13):
%            a CONTENT TOKEN carries what its stream holds at a frame
%            (the code's "clean"); a QUERY TOKEN carries no content and
%            exists to predict a frame (the code's "query slot"). Say
%            "content token" only where the contrast is live. NOT
%            "clean", NOT "slot", NOT "row" (the mask matrix's) --
%            all rejected 2026-09-13. Note "query token" collides
%            mildly with the Q of query/key/value; that is tolerable
%            because a query token IS the row doing the reading, but do
%            not write "the query of a query token".
%   note     one of the simultaneous notes inside a token, up to the
%            polyphony cap. Deliberately NOT given a coined name: the
%            reader owns the word already. Two sub-tokens per note.
%   sub-token  an atom of a note: its program id or its pitch--duration.
%            The outer transformer never sees these. Now has a parent
%            to be "sub" of, which is half the reason for the change.
%
% PROVENANCE -- RECONCILED 2026-09-13 in the method.
% init_pretrained_into_duet_block_diffusion.py duplicates ONE pretrained
% single-stream CP-transformer into both streams' Q/K/V/O and replicates
% its dense FFN across the experts. There are NOT two separately
% pretrained backbones in any run so far. 3.1 now says LM_x = LM_y for
% this testbed and why (melody and chord are one modality), and 3.3
% states the initialisation without re-arguing it.
% STILL OPEN: the INTRODUCTION's framing of merging two INDEPENDENTLY
% pretrained models. The framework claim is fine -- the construction
% does not assume one model -- but "independently" overstates what was
% run, and the intro is where a reviewer forms the expectation.
%
% NO COMMITMENT LEVEL. The runs use K=1 with the masked state untrained,
% which collapses the K+1-round schedule to draft-then-refine. The level
% index k and its embedding are therefore gone from the exposition; the
% code still carries k_emb, indexed at 0 for the refine pass. If a
% multi-level variant is ever reported, the schedule comes back here.
%
% Preamble: \usepackage{amsmath} and NOTHING ELSE (2026-09-13, by
% request -- no extra packages). amsmath supplies align, cases, \text,
% \boldsymbol, \operatorname*; everything else these files use is base
% LaTeX.
% DO NOT reintroduce \mathbb or \varnothing: both need amssymb, which
% is not loaded. Set memberships are written as prose sizes instead
% ("one d-dimensional vector", "a single E x d router"), which is
% shorter anyway, and \emptyset replaces \varnothing.
% ---------------------------------------------------------------
\section{Method}
\label{sec:method}

\subsection{Setup}
\label{sec:setup}

Let $x = (x_1, \dots, x_T)$ and $y = (y_1, \dots, y_T)$ be two streams
observed on a common clock, whose steps we call \emph{frames}, and let
$\mathrm{LM}_x$ and $\mathrm{LM}_y$ be temporal Transformers pretrained
on each. At frame $t$ stream $x$ carries $x_t$ and stream $y$ carries
$y_t$; we call each of these a \emph{token} of its stream. A token is
not a single symbol: it has internal structure and variable length. A
local encoder collapses $x_t$ into one $d$-dimensional vector
$\mathbf{h}^{x}_t$, and a local decoder reconstructs
a token from one such vector, emitting its parts one at a time. Each
$\mathrm{LM}$ therefore runs on a fixed-width sequence: one vector per
token, whatever that token contains. Our goal is a single model of
the joint process $p([x_t, y_t] \mid x_{<t}, y_{<t})$, built from those
pretrained backbones and fitted on a small paired corpus.

In the instantiation we study, $x$ is the melody stream and $y$ the
chord stream --- so a token is a \emph{melody token} or a \emph{chord
token} --- and a frame is one sixteenth note long, holding one of each.
The parts of a token are its stream's simultaneous notes at that frame,
up to a fixed polyphony, each note written as a pair of
\emph{sub-tokens}: its program id, and one symbol for its pitch and
quantised duration. A note is
therefore a musical unit but not a computational one --- no component
of the model operates on it --- which is why it is the frame-level
vector, and not the note, that we call a token. Which stream is which
is arbitrary; no component below is specific to either role. Melody and
chord are two domains of one modality, so here
$\mathrm{LM}_x = \mathrm{LM}_y$: both backbones are instantiated from a
single pretrained symbolic-music model, which is already a competent
generator of either stream. Nothing in the construction assumes this,
and where the streams are genuinely distinct modalities each backbone
is taken from its own pretrained model.

\subsection{Interleaved Input Streams}
\label{sec:interleave}

We place the two streams in one sequence, alternating by frame,
\begin{equation}
  \mathbf{h}
  = \bigl[\,\mathbf{h}^{x}_1,\ \mathbf{h}^{y}_1,\
            \mathbf{h}^{x}_2,\ \mathbf{h}^{y}_2,\ \dots\,\bigr],
  \label{eq:interleave}
\end{equation}
where the position $p = 1, \dots, 2T$ indexes the sequence. This
\emph{position} is an absolute index into the $2T$ tokens, not a time
index: each frame contributes two of them, odd $p$ carrying stream $x$
and even $p$ stream $y$, and frame $t$ occupies positions $2t{-}1$ and
$2t$.

The model reads this sequence shifted one frame --- two positions ---
to the right, in the usual autoregressive way. Position $p$ therefore
\emph{holds} what position $p{-}2$ carries, which is its own stream one
frame earlier, since shifting by two preserves parity; and it
\emph{predicts} what position $p$ itself carries, stream
$s \in \{x, y\}$ at frame $\lceil p/2 \rceil$, which we call $p$'s
\emph{prediction frame}. The rotary embedding is applied at that
prediction frame rather than at $p$, so the two tokens of a frame share
one rotary position: simultaneity is encoded as distance zero.
Interleaving rather than concatenating is what makes $[x_t, y_t]$ a
temporal unit: the two streams share one position axis and one clock,
and neither is a block appended to the other.

\subsection{Attention in a Two-backbone Model}
\label{sec:attention}

\sys uses two parallel backbones with stream-specific projections
$\{\mathbf{W}^{Q}_{s}, \mathbf{W}^{K}_{s}, \mathbf{W}^{V}_{s},
\mathbf{W}^{O}_{s}\}$, $s \in \{x, y\}$, applied to their respective
positions. Both backbones, including their attention projections and
feed-forward layers, are initialized from the pretrained model of their
stream --- here, as Sec.~\ref{sec:setup} notes, one model copied twice.
The feed-forward configuration is detailed in
Sec.~\ref{sec:permod_moe}.

We design the attention masks carefully since they define the
dependencies within and between streams. Let $s(p)$ denote the stream
at position $p$ and $t(p) = \lceil p/2 \rceil$ its prediction frame.
Attention comprises three passes over shared queries, keys and values,
each independently normalized over a distinct key set:
\begin{align}
  \mathcal{K}_{\text{intra}}(p)
    &= \{\, q : s(q) = s(p),\ t(q) \le t(p) \,\},
    \label{eq:kintra}\\
  \mathcal{K}_{\text{cross}}(p)
    &= \{\, q : s(q) \neq s(p),\ t(q) < t(p) \,\},
    \label{eq:kcross}\\
  \mathcal{K}_{\text{frame}}(p)
    &= \{\, q : s(q) \neq s(p),\ t(q) = t(p) \,\}.
    \label{eq:kframe}
\end{align}
\emph{Intra-stream attention} preserves the pretrained backbone's
causal attention independently within each stream, corresponding to a
stride of two in the interleaved sequence. \emph{Cross-stream
attention} conditions each stream on the other at earlier prediction
frames, without requiring an additional sequential decoding step.
\emph{Within-frame attention} connects the two positions predicting the
same frame bidirectionally, supporting concurrent prediction of $x_t$
and $y_t$. Although incompatible with index-based causal masking over
Eq.~\eqref{eq:interleave}, this connection preserves autoregressive
causality, because neither of the two positions contains what it
predicts: under the shift, the one predicting $x_t$ holds $x_{t-1}$ and
the one predicting $y_t$ holds $y_{t-1}$. Reading each other exposes
frame $t{-}1$, never frame $t$.

The three sets partition every position whose prediction frame is at
most $t(p)$, and their masks are fixed functions of position parity and
index. What is learned is how much of each readout to admit. With
$\mathbf{h}_p$ the block's input at position $p$,
\begin{equation}
  \mathbf{o}_p = \mathbf{W}^{O}_{s}\bigl(
    \mathbf{u}_{\text{intra},p}
    + g^{c}_{s}(\mathbf{h}_p)\,\mathbf{u}_{\text{cross},p}
    + g^{f}_{s}(\mathbf{h}_p)\,\mathbf{u}_{\text{frame},p}\bigr),
  \label{eq:gates}
\end{equation}
where $g^{c}_{s}, g^{f}_{s} \in (0,1)$ are per-stream \emph{scalars} ---
one value per position for each pathway, applied to every dimension of
the readout, so a gate sets how much enters and never which part of it.

Both gate biases are initialised large and negative, so at step zero
the block is \emph{exactly} the pretrained attention, and the paired
corpus is spent on \emph{when} the streams read each other rather than
on how to generate either. The sub-layer closes as
$\tilde{\mathbf{h}}_p = \mathrm{LN}(\mathbf{h}_p + \mathbf{o}_p)$.

\subsection{Role specialization via per-stream expert routing}
\input{method_per_part_moe}

\subsection{Order-free joint prediction}
\label{sec:querytok}

Eq.~\eqref{eq:interleave} leaves one ordering problem. Within frame $t$
the interleaved sequence still places $x_t$ before $y_t$, so an
autoregressive decode would draw $x_t$, commit it, and condition $y_t$
on the committed value --- the fixed direction we set out to remove. We
resolve this by appending to the sequence two \emph{query tokens}, one
per stream, both assigned prediction frame $t$. Every token so far has
been a \emph{content token}, carrying what its stream holds at a frame;
a query token carries no content of its own and exists only to predict
a frame. They are appended after every content token, but are given the
rotary index $t{+}1$, the phase frame $t$ occupies in the shifted
sequence, so each sits where the frame it predicts belongs rather than
at the end. Each attends to the committed past of both streams and to
\emph{the other query token}, and to nothing else. No content token
ever attends to a query token, so the query tokens stay outside the
autoregressive pathway. The two are symmetric by construction: they see
the same past and one another, and neither is downstream of the other.

Frame $t$ is then produced in two passes. The first is a \emph{draft}:
one forward yields $\tilde{x}_t, \tilde{y}_t$ from the autoregressive
heads of the two content tokens whose prediction frame is $t$, already
coupled through $\mathcal{K}_{\text{frame}}$ but with neither token
observed by the other. The second is a \emph{refinement}: the drafts
are encoded into the query tokens as
\begin{equation}
  \mathrm{query}_s \;\leftarrow\; \mathrm{enc}(\tilde{s}_t)
  + \mathbf{q}_s,
  \qquad s \in \{x, y\},
  \label{eq:queryinput}
\end{equation}
with $\mathbf{q}_s$ a learned per-stream query embedding, and a second
forward emits the committed $x_t, y_t$ from the two query tokens. Both
are written and read out together, so neither stream ever observes the
other's final token for frame $t$: the frame is negotiated rather than
ordered. The construction admits further passes by re-encoding the
committed estimates; we use one throughout.

Training must put in the query tokens the same content decoding will,
so each step runs two forwards over the same batch. The first is a
no-grad probe whose autoregressive heads produce the draft, sampled at
temperature $1$ to match the commitment the decode makes. The second
writes Eq.~\eqref{eq:queryinput} into the query tokens and is
supervised, with cross-entropy on both of them against the true frame
$t$ alongside the ordinary autoregressive loss on the content tokens.
The draft is not a corrupted ground truth but a sample from the model's
own predictive distribution --- wrong in the way the model is wrong ---
which is exactly the state the query tokens meet at inference and which
no corruption process can manufacture. Both are drafted on every step,
so training never visits a state in which one stream is resolved and
the other is not.

% ---------------------------------------------------------------
% TO VERIFY before submission:
%  * SCHEME: the RoPE paragraph describes time_rope_aligned (v1.2).
%    train_duet_block_diffusion.sbatch defaults TIME_ROPE_ALIGNED=0,
%    which is v1.1 -- clean positions keep rotary == p and only the
%    slots are re-indexed. CHECK the reported run's directory name:
%    v1.2 matches this text, v1.1 does not.
%  * "sampled at temperature 1" = SC_DRAFT_TEMP=1.0; confirm against
%    the final run's log line.
%  * "we use one [refinement pass] throughout" = A3_SCHEDULE=refine
%    with A3_REFINE_STEPS=1; confirm against the E1 decode config.
%    (RESOLVED 2026-09-13: \ref{sec:routing} is gone. It was cited only
%    by the shared-router exposition, which 3.4 no longer carries.)
%  * Hyperparameters (12 global layers, E=4, k=2, d_f=3d) belong in the
%    experimental setup section, not here.
% ---------------------------------------------------------------
