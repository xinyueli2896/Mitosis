% ---------------------------------------------------------------
% Methodology: role specialization via per-stream expert routing.
% Terminology per EXPERIMENTS.md Decision 1 as amended 2026-09-04:
% the two are STREAMS (x and y in the general setup, melody and
% harmony in the testbed); "modality" belongs to the general framework
% and the positioning clause only. This file said "part" throughout
% until 2026-09-12, from the pre-amendment rule.
% Matches SimpleMoEFFN (cp_transformer_m2c_jointattn.py):
% E=4 experts, top-k=2, routers bias-free, Switch aux at 0.01,
% post-LN residual, gate_c seeded from gate_m at warm start.
% NO \paragraph HEADINGS (2026-09-12, by request).
% NOTATION follows method.tex's terminology block: rows are indexed by
% p, hidden state h_p, layer output z_p, router probabilities pi_p. x
% and y are STREAM NAMES and must not double as vectors, and the routed
% unit is a ROW -- "token" is reserved for the atoms inside a frame,
% which this layer never sees.
% Preamble requirements: amsmath, amssymb. (bbm no longer needed --
% the indicator went with the balance equation.)
% ---------------------------------------------------------------
\label{sec:permod_moe}

Let $\mathbf{h}_p \in \mathbb{R}^{d}$ denote the hidden state of row
$p$ of the interleaved sequence, and let $s_p \in \{x, y\}$ be the
\emph{stream label} of that row (the two streams alternate
by frame; the appended query slots inherit the label of the stream
they predict). Every transformer block ends in a sparsely gated
mixture-of-experts (MoE) feed-forward layer with a pool of $E$ experts
\begin{equation}
  \mathrm{FFN}_i(\mathbf{h})
  \;=\;
  \mathbf{W}^{(2)}_i\,
  \sigma\!\bigl(\mathbf{W}^{(1)}_i \mathbf{h}\bigr),
  \qquad i = 1, \dots, E,
  \label{eq:expert}
\end{equation}
where $\mathbf{W}^{(1)}_i \in \mathbb{R}^{d_f \times d}$,
$\mathbf{W}^{(2)}_i \in \mathbb{R}^{d \times d_f}$, and $\sigma$ is
the GELU nonlinearity.

The conventional layer scores every row with a single router
$\mathbf{G} \in \mathbb{R}^{E \times d}$,
\begin{equation}
  \boldsymbol{\pi}_p = \operatorname{softmax}\bigl(\mathbf{G}\,
  \mathbf{h}_p\bigr) .
  \label{eq:shared_router}
\end{equation}
Because the per-stream attention projections imprint the stream label
into the hidden state, $\mathbf{h}_p$ decomposes as
$\mathbf{h}_p = \mathbf{c}_p + \mathbf{s}_{s_p}$, a content component
plus a stream-conditional offset (the ``stamp''), and the router's
logits inherit an identity term $\mathbf{G}\,\mathbf{s}_{s_p}$ that
separates the two streams regardless of content. In our routing
analysis (Sec.~\ref{sec:routing}) this identity term accounts for the
majority of the shared router's expert separation: the router spends
its capacity re-deriving \emph{which stream a row belongs to} --- a bit
the input already carries --- rather than \emph{what the row
contains}.

We therefore give each stream its own router,
$\mathbf{G}^{(x)}, \mathbf{G}^{(y)} \in \mathbb{R}^{E \times d}$, and
score every row with the router of its own stream:
\begin{equation}
  \boldsymbol{\pi}_p = \operatorname{softmax}\bigl(
  \mathbf{G}^{(s_p)}\,\mathbf{h}_p\bigr)
  = \operatorname{softmax}\bigl(
  \mathbf{G}^{(s_p)}\mathbf{c}_p
  + \mathbf{G}^{(s_p)}\mathbf{s}_{s_p}\bigr).
  \label{eq:permod_router}
\end{equation}
Within the population a given router ever scores, the stamp term
$\mathbf{G}^{(s)}\mathbf{s}_{s}$ is a \emph{constant} logit offset: it
can bias which experts a stream prefers overall, but it cannot make
two rows of the same stream route differently. All routing
\emph{variation} within a stream is thus content-driven by
construction, and the identity bit stops consuming router capacity.

Crucially, the expert pool itself remains \emph{shared and
unassigned}: both routers score the same $E$ experts, so which experts
a stream recruits --- and whether any expert serves both streams,
acting as an integrator --- is learned rather than imposed. This is the
designed contrast with hard-partitioned designs (stream-specific
experts in the sense of MoMa~\cite{moma} or VL-MoE), which we ablate by
masking each stream to a disjoint half of the pool,
\begin{equation}
  \tilde{g}_{p,i} =
  \begin{cases}
    \bigl[\mathbf{G}\,\mathbf{h}_p\bigr]_i, &
      i \in \mathcal{P}(s_p),\\[2pt]
    -\infty, & \text{otherwise},
  \end{cases}
  \label{eq:hardroute}
\end{equation}
with $\mathcal{P}(x) \cap \mathcal{P}(y) = \varnothing$: same
parameters and activated compute, but an integrator is no longer
representable.

Each row is dispatched to its top-$k$ experts
$\mathcal{S}_p = \operatorname{top}\text{-}k(\boldsymbol{\pi}_p)$ with
renormalized weights, and the layer output is the residually
connected mixture
\begin{equation}
  \mathbf{z}_p = \mathrm{LN}\Bigl(\mathbf{h}_p +
  \sum_{i \in \mathcal{S}_p}
  \frac{\pi_{p,i}}{\sum_{j \in \mathcal{S}_p} \pi_{p,j}}\,
  \mathrm{FFN}_i(\mathbf{h}_p)\Bigr).
  \label{eq:dispatch}
\end{equation}
Load is balanced with the standard auxiliary term of
\cite{fedus2022switch}, $\mathcal{L}_{\mathrm{bal}} = E \sum_{i} f_i
P_i$, where $f_i$ is the fraction of a batch's rows that take expert
$i$ as their top choice and $P_i$ their mean gate probability for it;
it is averaged across layers and added to the training loss with
weight $\lambda_{\mathrm{bal}} = 0.01$. In the hard-partition ablation
it is computed \emph{within each pool} and averaged, since the
all-expert form would demand a uniform load that the masked routing
cannot produce, penalizing that arm for its own architecture.

The change is cheap and exactly localised. Replacing
Eq.~\eqref{eq:shared_router} with Eq.~\eqref{eq:permod_router} adds
one bias-free $E \times d$ routing
matrix per layer ($Ed$ parameters, ${\approx}10^{-3}$ of the layer)
and changes neither the expert pool, the number of experts activated
per row ($k$ of $E$), nor any other computation, so comparisons
against the shared-router and hard-partition arms are at parameter
and compute parity. At initialization the two routers are copies of
the pretrained shared router, making the first optimization step
identical to the shared-gate model's; they diverge only as their
streams' gradients differ. We use $E = 4$, $k = 2$, $d_f = 3d$
throughout.
% ---------------------------------------------------------------
% BibTeX entry expected by \cite{fedus2022switch}:
% @article{fedus2022switch, title={Switch transformers: Scaling to
%   trillion parameter models with simple and efficient sparsity},
%   author={Fedus, William and Zoph, Barret and Shazeer, Noam},
%   journal={JMLR}, volume={23}, number={120}, pages={1--39},
%   year={2022}}
% ---------------------------------------------------------------
